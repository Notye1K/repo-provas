import styled from 'styled-components'

const Div = styled.div`
    .term,
    .disci {
        font-family: 'Poppins', sans-serif;
        font-style: normal;
        font-weight: 500;
        font-size: 18px;
        line-height: 24px;
    }
    .category {
        font-family: 'Poppins', sans-serif;
        line-height: 24px;
        font-weight: 400;
    }
    .test {
        color: gray;
    }
`

export default Div
